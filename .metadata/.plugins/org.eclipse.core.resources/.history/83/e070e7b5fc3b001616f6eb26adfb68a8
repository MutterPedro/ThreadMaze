package br.com.maze;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;

public class Player extends Thread{
	
	private String playerName;
	private int posx;
	private int posy;
	
	public Player(String name) {
		super();
		this.playerName = name;
	}
	
	public Player() {
		super();
		this.playerName = "player"+this.getId();
	}

	public String getPlayerName() {
		return playerName;
	}

	public void setPlayerName(String playerName) {
		this.playerName = playerName;
	}

	@Override
	public void run(){
		File file = new File(this.getPlayerName()+"_log.txt");
		
		try {
			if(!file.exists())
				file.createNewFile();
			
			FileWriter writer = new FileWriter(file);
			for(int i=0;i<MazeConfig.MAZEMAP.length;i++){
				for(int j=0;j<MazeConfig.MAZEMAP.length;i++){
					if(MazeConfig.MAZEMAP[i][j].equals("i")){
						this.posx = i;
						this.posy = j;
						MazeConfig.MAZEMAP[i][j] = this.getId();
					}
				}
			}
		} catch (IOException e) {			
			e.printStackTrace();
		}
		
	}
}
